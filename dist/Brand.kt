// GENERATED by scripts/generate.mjs from css/tokens.css.
// Do not edit. Re-run the generator; `--check` proves this file current.
//
// ⚠️ THE FLUID TYPE SCALE DOES NOT PORT. On the web the display rungs are
// clamp(min, Nvw, max) — a viewport-relative middle term with no native
// equivalent. Only min and max are emitted. Pick with the platform's own
// accessibility scale (Dynamic Type / sp); do not hard-code the max and
// call it done.
//
// ⚠️ ACCENT VALUES ARE PER SITE and only --accent-soft derives. A native
// app supplies --accent-ink and --accent-lift itself, exactly as each
// site's brand.css does. See accentRule in dist/tokens.json.

package eco.three33.brand

import androidx.compose.ui.graphics.Color
import androidx.compose.ui.graphics.lerp
import androidx.compose.ui.unit.dp

object Brand {
    const val VERSION = "1.3.0"

    /** The B-Gem media-type palette. A colour here MEANS a medium.
     *  Not a site palette, and not the six-TLD rainbow. */
    enum class Gem(val hex: String, val media: String, val color: Color) {
        RUBY("#c8102e", "video", Color(0xFFC8102E)),
        FIRE_OPAL("#e85d04", "voice / audio", Color(0xFFE85D04)),
        CITRINE("#f0b429", "photo", Color(0xFFF0B429)),
        EMERALD("#00a36c", "animation", Color(0xFF00A36C)),
        SAPPHIRE("#0f52ba", "link", Color(0xFF0F52BA)),
        AMETHYST("#9966cc", "letter / text", Color(0xFF9966CC)),
        DIAMOND("#f4f7ff", "album / collection", Color(0xFFF4F7FF));
    }

    // Pink is ABSENT BY CONSTRUCTION — reserved for B-Dating. Not an oversight.

    // The AURA RAMP and the SIX-TLD RAINBOW are deliberately not emitted here:
    // the first has only a web consumer so far, the second has no pinned values
    // anywhere. Named as absent rather than forgotten. See the auras and
    // tlds blocks in dist/tokens.json.

    data class Palette(
        val bg: Color,
        val bgTint: Color,
        val surface: Color,
        val surface2: Color,
        val line: Color,
        val lineStrong: Color,
        val ink: Color,
        val inkDim: Color,
        val inkFaint: Color,
        val scrim: Color
    )

    val dark = Palette(
        bg = Color(0xFF0A0A0F),
        bgTint = Color(0xFF191426),
        surface = Color(0xFF17171D),
        surface2 = Color(0xFF1F1F27),
        line = Color(0xFF000000),
        lineStrong = Color(0xFF000000),
        ink = Color(0xFFF5F5F7),
        inkDim = Color(0xFF9B9BA5),
        inkFaint = Color(0xFF6B6B76),
        scrim = Color(0xFF000000)
    )

    val light = Palette(
        bg = Color(0xFFFCFBFE),
        bgTint = Color(0xFFF4F1FB),
        surface = Color(0xFFF4F1FB),
        surface2 = Color(0xFFEBE7F5),
        line = Color(0xFFFFFFFF),
        lineStrong = Color(0xFFFFFFFF),
        ink = Color(0xFF16141C),
        inkDim = Color(0xFF5B5766),
        inkFaint = Color(0xFF8B8796),
        scrim = Color(0xFFFFFFFF)
    )

    object Radius {
        val control = 16.dp
        val card = 20.dp
        val input = 16.dp
        val pill = 999.dp
    }

    object Control {
        val height = 56.dp
        val heightSmall = 36.dp
    }

    /** The heartbeat. Two UNEQUAL beats then a long rest. THAT MORPHOLOGY is
     *  the signature, not the cadence — a 72 BPM variant was built and
     *  rejected. Under reduced motion: stop, and substitute nothing. */
    object Beat {
        const val DURATION_MS = 1500
        const val DELAY_MS = 0
        val STEPS = listOf(
            0f to 1f,
            0.14f to 1.3f,
            0.28f to 1f,
            0.42f to 1.3f,
            0.7f to 1f,
            1f to 1f
        )
    }

    /** THE METTA LIGHT — the session descent. One scalar in, one colour out:
     *  t is 0 at session start and 1 at session end, passing through the seven
     *  gems in order. Guards travel with it: no numerals, no totals, no
     *  streaks, no ranks. The breath is NOT the heartbeat, and carries no
     *  claim. */
    /** ⚠️ Samples of an OKLAB curve, not the seven stops — lerping two gems in
     *  sRGB differs from the web through the middle of every segment. */
    val METTA_RAMP = listOf(
        Color(0xFFC8102E),
        Color(0xFFD02C2A),
        Color(0xFFD83F23),
        Color(0xFFE04E19),
        Color(0xFFE85D04),
        Color(0xFFEB750F),
        Color(0xFFEE8B19),
        Color(0xFFEFA021),
        Color(0xFFF0B429),
        Color(0xFFC7B246),
        Color(0xFF9CAE57),
        Color(0xFF69A963),
        Color(0xFF00A36C),
        Color(0xFF009285),
        Color(0xFF007F99),
        Color(0xFF006BAA),
        Color(0xFF0F52BA),
        Color(0xFF4159BF),
        Color(0xFF615EC3),
        Color(0xFF7E63C8),
        Color(0xFF9966CC),
        Color(0xFFAF8BDA),
        Color(0xFFC6AFE7),
        Color(0xFFDDD3F4),
        Color(0xFFF4F7FF)
    )

    fun metta(t: Float): Color {
        val p = t.coerceIn(0f, 1f) * (METTA_RAMP.size - 1)
        val i = p.toInt().coerceAtMost(METTA_RAMP.size - 2)
        return lerp(METTA_RAMP[i], METTA_RAMP[i + 1], p - i)
    }

    /** ⚠️ PLACEMENT RULE: a beating mark goes on CHROME and never beside a
     *  person's name. */

    /** The B-Emblem path, viewBox 0 0 24 24. The 45° rotation is BAKED INTO
     *  THE COORDINATES — never re-apply a rotation transform. */
    const val EMBLEM_PATH = "M3.9743 20.0257L3.8824 18.0670C3.5430 11.1232 3.3167 6.5411 5.9896 3.8683C8.1675 1.6904 11.5899 1.6904 13.7678 3.8683C14.9981 5.0986 15.6062 6.8523 15.4719 8.5281C17.1477 8.3938 18.9014 9.0019 20.1317 10.2322C22.3096 12.4101 22.3096 15.8325 20.1317 18.0104C17.4589 20.6833 12.8768 20.4570 5.9260 20.1247L3.9743 20.0257Z"
}
